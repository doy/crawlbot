#!/usr/bin/env perl
use strict;
use warnings;
use lib 'lib';
use Crawl::Bot;

Crawl::Bot->new(
    server   => 'irc.freenode.net',
    channels => ['##crawl-dev', '##crawl'],
    nick     => 'Cheibriados',
    name     => 'Cheibriados the Crawl Bot',
)->run;
